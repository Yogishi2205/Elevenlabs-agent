# Render Blueprint: VoiceOps static site (HTML + CSS + JS)
# Docs: https://render.com/docs/blueprint-spec
#
# On Render → New → Blueprint:
#   Set "Blueprint Path" to exactly: ELevenlabs-agent-voiceOpsrender.yaml

services:
  - type: web
    name: elevenlabs-voiceops
    runtime: static
    # Do not set `plan: free` here — Render rejects it for web/static; pick Free in the UI or omit plan.
    buildCommand: "echo 'Static files — no build'"
    staticPublishPath: .
